<div class="container">
  <div class="row">
    <div>
      <%= flash[:notice] %>
    </div>
  </div>

  <div class="row my-4">
    <%= render 'homes/sidebar' %>

    <div class="col-md-8 offset-md-1">
      <p>
        ようこそ、ながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。
      </p>

      <p><strong>新着商品</strong></p>

      <div class="row my-4">
        <% @items.each do |item| %>
          <div class="col-md-3">
            <%= link_to item_path(item.id) do %>
              <%= image_tag item.get_image(100,100) %>
            <% end %>
            <p><%= item.name %></p>
            <p>￥<%= item.price %></p>
          </div>
        <% end %>
      </div>

      <%= link_to "全ての商品を見る", items_path %>

    </div>
  </div>
</div>
